diff --git a/dist/index.es.js b/dist/index.es.js
index 193ea6f4991a60c0fc996579d8d4a52e627725cf..82f2e59a17ab77519cff5acac7366a1c26b78f05 100644
--- a/dist/index.es.js
+++ b/dist/index.es.js
@@ -108,6 +108,7 @@ var FLUSH_DELAY = 200;
 var debug = function debug() {};
 // Type guard to check if a value is a DataTransfer
 var isDataTransfer = value => (value === null || value === void 0 ? void 0 : value.constructor.name) === 'DataTransfer';
+var SLATE_ANDROID_HANGUL_PATCH_ID = 'composition-anchor-v2';
 function createAndroidInputManager(_ref) {
   var {
     editor,
@@ -120,6 +121,29 @@ function createAndroidInputManager(_ref) {
   var actionTimeoutId = null;
   var idCounter = 0;
   var insertPositionHint = false;
+  var compositionAnchor = null;
+  var resolveAndroidHangulCompositionDiff = (path, data) => {
+    if (!compositionAnchor || !Path.equals(compositionAnchor.path, path) || !data) {
+      return null;
+    }
+    var leaf = Node.leaf(editor, path);
+    var pendingDiffs = EDITOR_TO_PENDING_DIFFS.get(editor);
+    var pendingOnPath = pendingDiffs === null || pendingDiffs === void 0 ? void 0 : pendingDiffs.find(change => Path.equals(change.path, path));
+    var effectiveText = pendingOnPath ? applyStringDiff(leaf.text, pendingOnPath.diff) : leaf.text;
+    var committedPrefix = effectiveText.slice(0, compositionAnchor.offset);
+    if (compositionAnchor.offset > 0 && data.startsWith(committedPrefix)) {
+      return {
+        start: 0,
+        end: effectiveText.length,
+        text: data
+      };
+    }
+    return {
+      start: compositionAnchor.offset,
+      end: effectiveText.length,
+      text: data
+    };
+  };
   var applyPendingSelection = () => {
     var pendingSelection = EDITOR_TO_PENDING_SELECTION.get(editor);
     EDITOR_TO_PENDING_SELECTION.delete(editor);
@@ -246,13 +270,38 @@ function createAndroidInputManager(_ref) {
     if (compositionEndTimeoutId) {
       clearTimeout(compositionEndTimeoutId);
     }
+    var endData = _event.data;
+    if (endData && compositionAnchor && Editor.hasPath(editor, compositionAnchor.path)) {
+      var path = compositionAnchor.path;
+      var resolved = resolveAndroidHangulCompositionDiff(path, endData);
+      var leaf = Node.leaf(editor, path);
+      if (resolved && leaf.text !== endData) {
+        storeDiff(path, resolved);
+        flush();
+      }
+    }
     compositionEndTimeoutId = setTimeout(() => {
       IS_COMPOSING.set(editor, false);
+      compositionAnchor = null;
       flush();
     }, RESOLVE_DELAY);
   };
   var handleCompositionStart = _event => {
     IS_COMPOSING.set(editor, true);
+    if (editor.selection) {
+      compositionAnchor = _objectSpread$8({}, editor.selection.anchor);
+    } else {
+      var window = ReactEditor.getWindow(editor);
+      var domSelection = window.getSelection();
+      var slateRange = domSelection && domSelection.rangeCount > 0 ? ReactEditor.toSlateRange(editor, domSelection, {
+        exactMatch: false,
+        suppressThrow: true
+      }) : null;
+      compositionAnchor = slateRange ? _objectSpread$8({}, slateRange.anchor) : {
+        path: [0, 0],
+        offset: 0
+      };
+    }
     if (compositionEndTimeoutId) {
       clearTimeout(compositionEndTimeoutId);
       compositionEndTimeoutId = null;
@@ -603,6 +652,14 @@ function createAndroidInputManager(_ref) {
               end: _end.offset,
               text: _text
             };
+            // COMPAT: Android Firefox Hangul sends cumulative insertCompositionText with a
+            // collapsed DOM selection after the jamo leaf; replace from composition start.
+            if (type === 'insertCompositionText' && IS_COMPOSING.get(editor) && compositionAnchor && Path.equals(compositionAnchor.path, _start.path)) {
+              var resolved = resolveAndroidHangulCompositionDiff(_start.path, _text);
+              if (resolved) {
+                _diff = resolved;
+              }
+            }
             // COMPAT: Swiftkey has a weird bug where the target range of the 2nd word
             // inserted after a mark placeholder is inserted with an anchor offset off by 1.
             // So writing 'some text' will result in 'some ttext'. Luckily all 'normal' insert
@@ -638,7 +695,7 @@ function createAndroidInputManager(_ref) {
               if (currentSelection) {
                 var newPoint = {
                   path: _start.path,
-                  offset: _start.offset + _text.length
+                  offset: _diff.start + _text.length
                 };
                 scheduleAction(() => {
                   Transforms.select(editor, {
@@ -4112,5 +4169,5 @@ var withReact = function withReact(editor) {
   return e;
 };
 
-export { DefaultElement, DefaultLeaf, DefaultPlaceholder, DefaultText, Editable, ReactEditor, Slate, defaultScrollSelectionIntoView, useComposing, useEditor, useElement, useElementIf, useFocused, useReadOnly, useSelected, useSlate, useSlateSelection, useSlateSelector, useSlateStatic, useSlateWithV, withReact };
+export { DefaultElement, DefaultLeaf, DefaultPlaceholder, DefaultText, Editable, ReactEditor, SLATE_ANDROID_HANGUL_PATCH_ID, Slate, defaultScrollSelectionIntoView, useComposing, useEditor, useElement, useElementIf, useFocused, useReadOnly, useSelected, useSlate, useSlateSelection, useSlateSelector, useSlateStatic, useSlateWithV, withReact };
 //# sourceMappingURL=index.es.js.map
diff --git a/dist/index.js b/dist/index.js
index aa31a1a118a786fb141a20607e14deec55b4feb6..74ae64921f600b1305b385302fb97d96c616400d 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -237,6 +237,7 @@ var debug = function debug() {};
 var isDataTransfer = function isDataTransfer(value) {
   return (value === null || value === void 0 ? void 0 : value.constructor.name) === 'DataTransfer';
 };
+var SLATE_ANDROID_HANGUL_PATCH_ID = 'composition-anchor-v2';
 function createAndroidInputManager(_ref) {
   var editor = _ref.editor,
     scheduleOnDOMSelectionChange = _ref.scheduleOnDOMSelectionChange,
@@ -247,6 +248,31 @@ function createAndroidInputManager(_ref) {
   var actionTimeoutId = null;
   var idCounter = 0;
   var insertPositionHint = false;
+  var compositionAnchor = null;
+  var resolveAndroidHangulCompositionDiff = function resolveAndroidHangulCompositionDiff(path, data) {
+    if (!compositionAnchor || !slate.Path.equals(compositionAnchor.path, path) || !data) {
+      return null;
+    }
+    var leaf = slate.Node.leaf(editor, path);
+    var pendingDiffs = slateDom.EDITOR_TO_PENDING_DIFFS.get(editor);
+    var pendingOnPath = pendingDiffs === null || pendingDiffs === void 0 ? void 0 : pendingDiffs.find(function (change) {
+      return slate.Path.equals(change.path, path);
+    });
+    var effectiveText = pendingOnPath ? slateDom.applyStringDiff(leaf.text, pendingOnPath.diff) : leaf.text;
+    var committedPrefix = effectiveText.slice(0, compositionAnchor.offset);
+    if (compositionAnchor.offset > 0 && data.startsWith(committedPrefix)) {
+      return {
+        start: 0,
+        end: effectiveText.length,
+        text: data
+      };
+    }
+    return {
+      start: compositionAnchor.offset,
+      end: effectiveText.length,
+      text: data
+    };
+  };
   var applyPendingSelection = function applyPendingSelection() {
     var pendingSelection = slateDom.EDITOR_TO_PENDING_SELECTION.get(editor);
     slateDom.EDITOR_TO_PENDING_SELECTION["delete"](editor);
@@ -371,13 +397,38 @@ function createAndroidInputManager(_ref) {
     if (compositionEndTimeoutId) {
       clearTimeout(compositionEndTimeoutId);
     }
+    var endData = _event.data;
+    if (endData && compositionAnchor && slate.Editor.hasPath(editor, compositionAnchor.path)) {
+      var path = compositionAnchor.path;
+      var resolved = resolveAndroidHangulCompositionDiff(path, endData);
+      var leaf = slate.Node.leaf(editor, path);
+      if (resolved && leaf.text !== endData) {
+        storeDiff(path, resolved);
+        flush();
+      }
+    }
     compositionEndTimeoutId = setTimeout(function () {
       slateDom.IS_COMPOSING.set(editor, false);
+      compositionAnchor = null;
       flush();
     }, RESOLVE_DELAY);
   };
   var handleCompositionStart = function handleCompositionStart(_event) {
     slateDom.IS_COMPOSING.set(editor, true);
+    if (editor.selection) {
+      compositionAnchor = _objectSpread$8({}, editor.selection.anchor);
+    } else {
+      var window = ReactEditor.getWindow(editor);
+      var domSelection = window.getSelection();
+      var slateRange = domSelection && domSelection.rangeCount > 0 ? ReactEditor.toSlateRange(editor, domSelection, {
+        exactMatch: false,
+        suppressThrow: true
+      }) : null;
+      compositionAnchor = slateRange ? _objectSpread$8({}, slateRange.anchor) : {
+        path: [0, 0],
+        offset: 0
+      };
+    }
     if (compositionEndTimeoutId) {
       clearTimeout(compositionEndTimeoutId);
       compositionEndTimeoutId = null;
@@ -766,6 +817,14 @@ function createAndroidInputManager(_ref) {
               end: _end.offset,
               text: _text
             };
+            // COMPAT: Android Firefox Hangul sends cumulative insertCompositionText with a
+            // collapsed DOM selection after the jamo leaf; replace from composition start.
+            if (type === 'insertCompositionText' && slateDom.IS_COMPOSING.get(editor) && compositionAnchor && slate.Path.equals(compositionAnchor.path, _start.path)) {
+              var resolved = resolveAndroidHangulCompositionDiff(_start.path, _text);
+              if (resolved) {
+                _diff = resolved;
+              }
+            }
             // COMPAT: Swiftkey has a weird bug where the target range of the 2nd word
             // inserted after a mark placeholder is inserted with an anchor offset off by 1.
             // So writing 'some text' will result in 'some ttext'. Luckily all 'normal' insert
@@ -801,7 +860,7 @@ function createAndroidInputManager(_ref) {
               if (currentSelection) {
                 var newPoint = {
                   path: _start.path,
-                  offset: _start.offset + _text.length
+                  offset: _diff.start + _text.length
                 };
                 scheduleAction(function () {
                   slate.Transforms.select(editor, {
@@ -4584,6 +4643,7 @@ exports.DefaultPlaceholder = DefaultPlaceholder;
 exports.DefaultText = DefaultText;
 exports.Editable = Editable;
 exports.ReactEditor = ReactEditor;
+exports.SLATE_ANDROID_HANGUL_PATCH_ID = SLATE_ANDROID_HANGUL_PATCH_ID;
 exports.Slate = Slate;
 exports.defaultScrollSelectionIntoView = defaultScrollSelectionIntoView;
 exports.useComposing = useComposing;
