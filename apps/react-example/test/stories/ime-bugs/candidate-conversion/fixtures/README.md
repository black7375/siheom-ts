# Hanja autocomplete conflict — capture fixtures

| Directory             | Meaning                                                                                |
| --------------------- | -------------------------------------------------------------------------------------- |
| `user-event/`         | `@testing-library/user-event` synthetic traces (`userEventTraces.test.tsx` snapshot)   |
| `macos-chrome-apple/` | OS capture slot: macOS + Chrome + Apple IME (`profileId`: `macos-chrome-apple`)        |

| File                    | `scenarioId` in JSON   | Notes |
| ----------------------- | ---------------------- | ----- |
| `broken-hanja-name.json` | `hanja-name-broken`   | 김→金→… 한자 변환 중 combobox가 키를 가로챔 |
| `fixed-hanja-name.json`  | `hanja-name-fixed`    | Same IME steps; combobox defers while composing |

OS profile JSON files are **empty shells** (`events: []`) — paste Storybook captures from **IME / Hanja Autocomplete Conflict**.

User-event JSON is generated by `userEventTraces.test.tsx` (not hand-edited).

Storybook capture: broken/fixed 모드 각각에서 「김태희」→「金泰熙」 한 글자씩 Option+Enter 한자 변환.
